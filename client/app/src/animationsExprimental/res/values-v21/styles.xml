<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="BaseAppTheme" parent="android:Theme.Material">

        <item name="android:windowActivityTransitions">true</item>

        <!-- specify enter and exit transitions -->
        <item name="android:windowEnterTransition">@transition/transition_window_enter</item>
        <item name="android:windowExitTransition">@transition/transition_window_exit</item>

        <!-- enable window content transitions -->
        <item name="android:windowContentTransitions">true</item>
        <!-- specify shared element transitions -->
        <item name="android:windowSharedElementEnterTransition">
            @transition/transition_window_share_element_enter
        </item>
        <item name="android:windowSharedElementExitTransition">
            @transition/transition_window_share_element_exit
        </item>
    </style>
</resources>