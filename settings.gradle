println("settings start")
include ':location', ':common', ':chatroom', ':network', ':storage', ':mockserver', ':image', ':uicomponent', ':login'
if (!isComponentMode.toBoolean()) {
    include ':app'
}
include ':gradle_plugin', ':annotations', ':processor'
println("settings end")
