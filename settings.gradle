println("settings start")
include ':location', ':common', ':chatroom', ':network', ':storage', ':mockserver', ':uicomponent'
if (!isComponentMode.toBoolean()) {
    include ':app'
}
include ':gradle_plugin', ':annotations', ':processor', ':networkExprimental'
println("settings end")
