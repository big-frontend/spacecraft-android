apply from: "${rootDir}/script/java_app.gradle"

apply plugin: "java-gradle-plugin"
gradlePlugin {
    plugins {

        proguardplugin {
            id = 'com.jamesfchen.proguard-plugin'
            implementationClass = 'com.jamesfchen.proguard.ProGuardPlugin'
        }
    }
}
dependencies {
    api project(':base-plugin')
}